version: '3.8'
services:
  postgres:
    image: postgres:16
    container_name: app-postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: appdb
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password